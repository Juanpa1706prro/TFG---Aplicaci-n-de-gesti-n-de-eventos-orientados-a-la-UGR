// -------------------------------------------------------------------
// In-map directions UI gate
// Set to false when Google authorization allows Routes API on MapLibre.
// Remove this file and map references once permanently enabled.
// -------------------------------------------------------------------

/**
 * When true, the map blocks «Cómo llegar» (in-app route) but keeps external Google Maps link.
 */
export const ROUTING_UI_BLOCKED = false;
