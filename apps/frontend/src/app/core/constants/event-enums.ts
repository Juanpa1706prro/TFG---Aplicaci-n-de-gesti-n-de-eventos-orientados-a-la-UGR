export enum EventManagerAssignmentRole {
  EDITOR = 'EDITOR',
  MODERATOR = 'MODERATOR',
}

/** Coincide con el backend: mapa y listados para todos vs. tutorías / sesión restringida. */
export enum EventVisibility {
  PUBLIC = 'public',
  PRIVATE = 'private',
}
